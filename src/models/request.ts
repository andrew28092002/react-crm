export type TRequest = {
  id: number
  name: string;
  phone: string;
  email: string;
  product: string;
  date: string;
  status: string
};

export type TNewRequest = {
  name: string;
  phone: string;
  email: string;
  product: string;
}
